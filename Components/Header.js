import {StyledHeader} from './Header.styled'

export default function Header(){
    return (
        <StyledHeader>
            <h1>
                League of Legends death counter
            </h1>
        </StyledHeader>
    )
}